apiVersion: v1
kind: Service
metadata:
  name: t3-chat-service
spec:
  type: NodePort
  selector:
    app: t3chat
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30007